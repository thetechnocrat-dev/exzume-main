var React = require('react');

var DataCollectorMenu = React.createClass({

  render: function () {
    return (
      <div>
        <h1 class="ui header">Data Collectors</h1>
      </div>
    );
  },

});

module.exports = DataCollectorMenu;
